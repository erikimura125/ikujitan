<h1>COOK</h1>
<p>料理に関する時短術</p>
<% @posts.each do |t| %>
  <% if t.category == "cook" %>
    <div>
      <%= link_to post_path(post.id) do %>
        <%= image_tag post.get_image %>
      <% end %>
      <%= post.title %>
    </div>
  <% end %>
<% end %>
